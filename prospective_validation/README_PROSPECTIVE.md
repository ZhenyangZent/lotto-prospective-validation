# 前瞻性鎖模驗證

正式模型固定為 `ProspectiveBatchLogistic-v1`。本階段不再以歷史或中途結果挑選模型；每期預測在官方開獎前產生，使用 canonical JSON 與 SHA-256 hash chain 保存。

只有已在開獎前建立且遠端 commit 已確認的預測，才計入100期與200期的正式樣本。每20期僅作描述性報告；第100期前禁止正式顯著性結論。

前瞻性實驗預測；尚未證明提高中獎機率。
